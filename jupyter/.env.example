# Auth token shared by JupyterLab and the MCP bridge. Generate:
#   openssl rand -hex 16
JUPYTER_TOKEN=change_me
# Host directory with the notebooks, mounted as /home/jovyan/work.
WORK_DIR=/path/to/notebooks
